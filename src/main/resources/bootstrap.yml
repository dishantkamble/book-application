spring:
  application:
    name: book-application
  cloud:
    config:
      uri:
      - http://localhost:8888
      discovery:
        enabled: true

# Commenting it out as configuring to generic Spring Discovery rather than Netflix's Eureka
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka