spring:
  cloud:
    config:
      name: book-application
      discovery:
        service-id: central-config-server
        enabled: true
 
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/